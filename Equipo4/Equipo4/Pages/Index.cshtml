@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<link rel="stylesheet" href="~/css/Index.css"/>

<div class="layout" id="menu">
    <div class="Productos">
        @foreach (var p in Model.Productos)
        {
            <div class="tarjeta">
                <img src="@p.ImagenUrl" alt="@p.Nombre" />
                <hr/>
                <small>@p.Sku</small>
                <h3>@p.Nombre</h3>
                <p>@p.Descripcion</p>
                <p>Precio: @p.Precio.ToString("C")</p> <!-- Currency -->
                <form method="post" asp-page-handler="Agregar">
                    <input type="hidden" name="sku" value="@p.Sku" />
                    <button type="submit" class="btn">Agregar al carrito</button>
                </form>
            </div>
        }
    </div>
    @if (Model.Carrito.Items != null && Model.Carrito.Items.Any())
    {
        <div class="carrito">
            <div class="carrito-encabezado">
                <img src="/images/carrito_compras.png" alt="Carrito de compras"/>
                <h1>Carrito</h1>
            </div>
            <hr>
            <div class="carrito-productos">
                @foreach (var item in Model.Carrito.Items)
                {
                    <div class="imagen">
                        <img src="@item.Producto.ImagenUrl" alt="@item.Producto.Nombre" />
                        <span>@item.Producto.Nombre</span>
                    </div>
                    <div class="detalles">
                        <span>Cantidad: @item.Cantidad</span>
                        <span>Precio: @item.Producto.Precio</span>
                        @if (item.Cantidad > 1)
                        {
                            <span>Subtotal: @item.Subtotal.ToString("C")</span>
                        }
                    </div>
                    <form method="post" asp-page-handler="Quitar">
                        <input type="hidden" name="sku" value="@item.Producto.Sku" />
                        <button type="submit" class="btn">Quitar</button>
                    </form>
                }
            </div>
            <hr>
            <div class="carrito-subtotal">
                <p>Subtotal: @Model.Carrito.GranSubtotal.ToString("C")</p>
                <p>IVA (16%): @Model.Carrito.Iva.ToString("C")</p>
            </div>
            <div class="carrito-total">
                <p><b>
                        Total: @Model.Carrito.Total.ToString("C")
                </b></p>
            </div>
            <hr>
            <button type="submit" class="btn" onclick="alert('¡¡¡FELICIDADES!!!')">Proceder al pago</button>
        </div>
    }
</div>